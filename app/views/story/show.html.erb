<div class='game'>

<div class='dayticker'>
  <h4>Day: <%= @story.day %></h4>
</div>

  <div class='column'>

    <div class='box'>
      <div class='eventlog'>
      <div class="events">
      <h3>Event Information</h3>
      </div>
         <p> <%= @event.setting %></p>
      </div>
    </div>

    <div class='box'>
      <div class='events'>
        <h4>Please choose your character for this event</h4>
      </div>
        <div id='event-form'>
          <%= form_tag("/story/#{@story.id + 1}", method: "get") do %>
          <p>Select X characters to X<p>
          <% @living_characters.each do |character| %>
            <%= check_box_tag character.id, character.id -%>
            <%= h character.name %>
          <% end %>
          <%= hidden_field_tag "day", @story.day %>
          <br><br>
          <%= submit_tag(value = "submit", id: "submit-button") %>
          <% end %>
        </div>
        <p class="event-success"><%= @event.success %></p>
        <p class="event-failure"><%= @event.failure %></p>
        <br>
        <%= link_to "next", "/story/#{@story.id + 1}", id: 'next-link' %>
    </div>

    <div class='box'>
      <div class='events'>
        <h4>Abilities</h4>
      </div>
      <div class='abilities_list'>
        <% if @abilities %>
          <% @abilities.each do |ability| %>
            <%= link_to ability.text, "/ability/#{ability.name}", id: "ability#{ability.id}" %>
          <% end %>
        <% end %>
      </div>
    </div>

  </div>

  <div class='characters'>
    <h3>Characters</h3>
    <% @characters.each do |character| %>
    <div class="characterbox">
    <p><%= character.name %> Attack: <%= character.attack %></p>
    <p>Morale: <%= character.morale %></p>
    </div>
    <% end %>
  </div>

</div>